services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dservices-socket
    restart: unless-stopped
    ports:
      - "3100:3100"
    networks:
      - global-network
    volumes:
      - .:/app
      - /app/node_modules
networks:
    global-network:
        external: true
